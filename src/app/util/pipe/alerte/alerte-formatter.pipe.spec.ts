import { AlerteFormatterPipe } from './alerte-formatter.pipe';

describe('AlerteFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new AlerteFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
